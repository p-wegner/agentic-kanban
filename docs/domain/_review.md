# Adversarial review report — domain docs

Generated by the `domain-docs` skill's Phase 4. Every module doc was reviewed by an
independent subagent instructed to **refute** it against the code (paraphrase sniff,
hallucinated requirements, missed core logic, wrong entry points, file-topology check,
evidence integrity, ubiquitous-language accuracy). `needs-fix` docs were then corrected
and their anchors re-verified.

`analyzed_sha`: 2cea8d3e · 12 modules · 6 sound on first pass, 6 fixed.

| Module | Verdict (initial) | Most significant defect found | Resolution |
|--------|-------------------|-------------------------------|------------|
| issues-board | needs-fix | Missed `status-view.ts` — the shared terminal/resolved/blocker-readiness predicates (#537, #784) the whole status-as-view narrative rests on; AI-Reviewed unblocks dependents (wider than terminal) | Added file + 3 rules; split board read-model invariant per endpoint; fixed 2 citations |
| workspaces | needs-fix | Mis-located the create→launch orchestrator (it's `workspace-create.service.ts:343`, not the repo); missed plan-mode auto-enable for high/critical priority + single-open-direct-workspace guard | Re-anchored Workflow #1; added both invariants |
| agent-providers | sound | "never-throws" overstated for the launch entry (`getProvider` throws on unknown); Pi `--approve` enforced by omission not version floor | Minor — accepted; (low-priority, left as flagged) |
| agent-sessions | sound | Launch-failure phrasing implied error-text precondition (it isn't one) | Minor — accepted |
| workflow-engine | needs-fix | **Fabricated enforcement**: `deriveStatusName` fallback invariant cited as enforced, but it has zero production callers (test-only); live path leaves status unchanged | Moved to "unwired helper" note; upgraded confirmed orphan-risk bug |
| monitor-orchestration | sound | Under-stated: `monitorShouldRun` + per-project scoping both bypass `resolveStartPolicy` (a project set to `monitor` in isolation may never schedule) — verifiable, not just inferred | Minor — accepted as a sharpening |
| review-merge | needs-fix | **Missed the actual merge enforcer** `pre-merge-gate.service.ts:47` (verify/smoke gate), miscredited to `review.service.ts`; gate runs only on exit/monitor paths, not manual merge | Added invariant + topology row + bypass-gap note; fixed frontmatter + offsets |
| butler | needs-fix | **False "dead code" claim**: the service fallback prompt IS live (the agent-questions recommender calls it); missed that whole consumer | Corrected; added recommender as downstream entry point; fixed backend-edit + event-feed scope |
| git-integration | sound | `git-info.service.ts` keeps its own `execGit` wrapper (not a 2nd spawn site, but undisclosed) | Minor — accepted |
| mcp-server | sound | "retries up to 3×" = actually 2 retries / 3 attempts; one delegation anchor loose | Minor — accepted |
| persistence-schema | needs-fix | **CRITICAL fabrication**: "only 2 FKs cascade, everything else NO ACTION" — false; many cascade/set-null FKs incl. `workflow_transitions→workspaces` | Restated FK landscape; added redundant-delete + existing-test citations; fixed wording |
| board-ui | sound | "client never blocks on its own rules" overstated — one real client-side hard gate (workspace-launch capacity); `activeAgentsTarget` feeds wrong consumer | Minor — accepted as sharpening |

## What the adversarial pass demonstrably caught
- **2 fabricated/mis-attributed enforcement claims** (workflow-engine `deriveStatusName`, persistence-schema FK cascades) — exactly the failure mode prose docs are prone to.
- **1 false "dead code" claim** (butler) that would have hidden a real second consumer.
- **2 mis-located cores** (workspaces orchestrator, review-merge pre-merge gate) — the doc was confident but pointing at the wrong file.
- Numerous **±1–2 line citation offsets** normalized.

The `sound` docs were genuinely sound: their reviewers verified 15–35 anchors each and
found only minor phrasing/precision issues, several of which were *sharpenings* of
already-correct findings rather than corrections.
